<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Automatización diaria: detectar solicitudes vencidas -->
        <record id="ir_cron_check_overdue_requests" model="ir.cron">
            <field name="name">Service Quote: comprobar solicitudes vencidas</field>
            <field name="model_id" ref="model_service_request"/>
            <field name="state">code</field>
            <field name="code">model._cron_check_overdue_requests()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).strftime('%Y-%m-%d 02:00:00')"/>
            <field name="doall" eval="False"/>
        </record>

    </data>
</odoo>
